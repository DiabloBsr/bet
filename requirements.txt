# Déploiement Streamlit Cloud (app cloud = streamlit_app.py).
# Le scraper local utilise en plus : playwright, pytest (voir .venv).
streamlit>=1.35.0
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0
SQLAlchemy>=2.0.0
python-dotenv>=1.0.0
# Lecture des cotes sur une capture (onglet a cette cote).
Pillow>=10.0.0
pytesseract>=0.3.10
